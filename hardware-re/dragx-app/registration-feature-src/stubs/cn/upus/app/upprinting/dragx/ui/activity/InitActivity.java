package cn.upus.app.upprinting.dragx.ui.activity;

// Compile-time-only stub. The REAL InitActivity.smali already exists in
// decoded_real_branded/ and is never touched by this feature -- this stub
// exists purely so javac can resolve `InitActivity.class` inside
// RegistrationGate.java (Intent's Class<?> constructor parameter needs
// some real, resolvable type at compile time). At the bytecode level, a
// reference to a class by fully-qualified name is just a symbolic type
// descriptor -- it does not embed this stub's (empty) implementation.
// After compiling+dexing+baksmali-ing (Task 4), ONLY RegistrationGate's
// and RegistrationActivity's generated smali are copied into the real
// decompile tree; this stub's own generated InitActivity.smali is
// discarded, never overwriting the real one.
public class InitActivity {
}
