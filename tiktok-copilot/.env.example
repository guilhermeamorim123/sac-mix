# --- IA ---
ANTHROPIC_API_KEY=sk-ant-...
# Modelo do classificador. claude-opus-5 e o padrao.
# Para cortar custo em live de alto volume, troque por claude-haiku-4-5.
COPILOT_MODEL=claude-opus-5

# --- Live ---
# @ da conta que vai transmitir (sem o @ tambem funciona)
TIKTOK_USERNAME=@mixconecta

# --- Auto-envio no chat (Playwright) ---
# ATENCAO: com STORE_BACKEND=supabase, quem manda nessas duas e a tabela
# `configuracoes` (aba Configuracoes do painel). As linhas abaixo valem so no
# modo local, ou enquanto o banco nao responde.
# false = so sugere no painel. true = digita sozinho no chat da live.
AUTO_REPLY_ENABLED=false
# Teto de mensagens automaticas por minuto. Nao suba muito: cadencia
# robotica e o que dispara deteccao.
AUTO_REPLY_MAX_PER_MIN=4
# Porta do Chrome em modo debug (o vendedor loga na mao, ver README)
CHROME_CDP_URL=http://localhost:9222

# --- Replay (live gravada sendo reexibida) ---
# true = exige check-in de supervisor para liberar o auto-envio
REPLAY_MODE=false
SUPERVISOR_INTERVAL_MIN=15

# --- Persistencia ---
# sqlite  = tudo local, catalogo vem do products.json (bom pro primeiro teste)
# supabase = painel le em tempo real; catalogo e configuracoes vem do banco
STORE_BACKEND=sqlite
SQLITE_PATH=copilot.db
SUPABASE_URL=https://xxxx.supabase.co
# A chave de SERVICO (service_role / sb_secret_...), nao a publicavel:
# o agente precisa passar por cima do RLS.
SUPABASE_SERVICE_KEY=eyJ...
# Identifica a loja. Precisa bater com uma linha de `sellers`.
SELLER_ID=mix-conecta
# De quanto em quanto tempo o agente rele catalogo e configuracoes do banco
# durante a live (o lojista pode importar planilha ou corrigir preco no meio).
REFRESH_SECONDS=30
